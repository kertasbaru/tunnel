#!/bin/bash
# //====================================================
# //  Newbie Store - Copy 2024
# //====================================================
# //  System Request : DEB 9-12 / UBUNTU 18-24
# //  Author         : Kurniawan Setiadi
# //  Develop        : Newbie Store
# //  email          : Newbiestore09@gmail.com
# //  telegram       : https://t.me/newbie_store24
# //====================================================
#Color
DF='\e[39m'
Bold='\e[1m'
Blink='\e[5m'
yell='\e[33m'
RED='\033[0;31m'
green='\e[32m'
PURPLE='\e[35m'
cyan='\e[36m'
LRED='\e[91m'
Lgreen='\e[92m'
Lyellow='\e[93m'
NC='\e[0m'
GREEN='\033[0;32m'
ORANGE='\033[0;33m'
LIGHT='\033[0;37m'
grenbo="\e[92;1m"
blue="\033[0;34m"
Blue="\033[36m"
g="\e[92;1m"
cyan='\e[36m'
r='\e[91m'
REDBG="\033[41m"  
WHITEBLD="\033[1;37m"
WHITE="\033[1;37m" 
NC="\033[0m"         
GREEN="\033[96;1m"
REDBLD="\033[0m\033[91;1m"
Green="\e[92;1m"
RED="\033[1;31m"
YELLOW="\033[33m"
BLUE="\033[36m"
GREENBG="\033[42;37m"
print_rainbow() {
    local text="$1"
    local warna_file="/etc/warna"

    # Inisialisasi warna default
    local start_r=0 start_g=255 start_b=150
    local mid_r=0 mid_g=150 mid_b=255
    local end_r=0 end_g=255 end_b=150

    # Fungsi untuk membaca nilai numerik dari file
    function get_value() {
        local key="$1"
        local value=$(grep -E "^$key=" "$warna_file" | cut -d'=' -f2 | tr -d '[:space:]')
        echo "${value:-0}"
    }

    # Jika file warna ada, baca isinya
    if [[ -f "$warna_file" ]]; then
        start_r=$(get_value "start_r")
        start_g=$(get_value "start_g")
        start_b=$(get_value "start_b")

        mid_r=$(get_value "mid_r")
        mid_g=$(get_value "mid_g")
        mid_b=$(get_value "mid_b")

        end_r=$(get_value "end_r")
        end_g=$(get_value "end_g")
        end_b=$(get_value "end_b")
    fi

    # Jalankan awk dengan warna yang sudah dimuat dari file
    echo "$text" | awk -v start_r="$start_r" -v start_g="$start_g" -v start_b="$start_b" \
                        -v mid_r="$mid_r" -v mid_g="$mid_g" -v mid_b="$mid_b" \
                        -v end_r="$end_r" -v end_g="$end_g" -v end_b="$end_b" '
    {
        len = length($0);
        for (i = 1; i <= len; i++) {
            progress = (i - 1) / (len - 1);
            if (progress < 0.5) {
                factor = progress * 2;
                r = int((1 - factor) * start_r + factor * mid_r);
                g = int((1 - factor) * start_g + factor * mid_g);
                b = int((1 - factor) * start_b + factor * mid_b);
            } else {
                factor = (progress - 0.5) * 2;
                r = int((1 - factor) * mid_r + factor * end_r);
                g = int((1 - factor) * mid_g + factor * end_g);
                b = int((1 - factor) * mid_b + factor * end_b);
            }
            printf "\033[38;2;%d;%d;%dm%s", r, g, b, substr($0, i, 1);
        }
        print "\033[0m";
    }'
}
garis(){
print_rainbow " ────────────────────────────────────────────────"
}
garis_bawah(){
print_rainbow " └──────────────────────────────────────────────┘"
}
garis_atas(){
print_rainbow " ┌──────────────────────────────────────────────┐"
}
garis_tebal(){
print_rainbow " ════════════════════════════════════════════════"
}
center() {
    BOX_WIDTH=48
    TEXT="$1"
    TEXT_LENGTH=${#TEXT}
    TEXT_CLEAN=$(echo -e "$TEXT" | sed 's/\x1b\[[0-9;]*m//g')
    TEXT_LENGTH=${#TEXT_CLEAN}
    # Hitung padding
    LEFT_PADDING=$(( (BOX_WIDTH - TEXT_LENGTH) / 2 ))
    RIGHT_PADDING=$(( BOX_WIDTH - TEXT_LENGTH - LEFT_PADDING ))

    # Cetak dengan warna menggunakan printf
    printf "%*s%s%*s\n" "$LEFT_PADDING" "" "$TEXT" "$RIGHT_PADDING" ""
}
function Newbie_Banner_Log() {
BOX_WIDTH=48
TEXT="[ LOG TIME BACKUP ] "
TEXT_LENGTH=${#TEXT}
LEFT_PADDING=$(( (BOX_WIDTH - TEXT_LENGTH) / 2 ))
RIGHT_PADDING=$(( BOX_WIDTH - TEXT_LENGTH - LEFT_PADDING ))
LEFT_SPACE=$(printf '%*s' $LEFT_PADDING '')
RIGHT_SPACE=$(printf '%*s' $RIGHT_PADDING '')
print_rainbow " ════════════════════════════════════════════════"
print_rainbow " ${LEFT_SPACE}${TEXT}${RIGHT_SPACE}"
print_rainbow " ════════════════════════════════════════════════"
}
function Newbie_Banner() {
BOX_WIDTH=48
TEXT="[ SHOW ALL SERVCIE SYSTEM ] "
TEXT_LENGTH=${#TEXT}
LEFT_PADDING=$(( (BOX_WIDTH - TEXT_LENGTH) / 2 ))
RIGHT_PADDING=$(( BOX_WIDTH - TEXT_LENGTH - LEFT_PADDING ))
LEFT_SPACE=$(printf '%*s' $LEFT_PADDING '')
RIGHT_SPACE=$(printf '%*s' $RIGHT_PADDING '')
print_rainbow " ════════════════════════════════════════════════"
print_rainbow " ${LEFT_SPACE}${TEXT}${RIGHT_SPACE}"
print_rainbow " ════════════════════════════════════════════════"
}
function Sc_Credit(){
nama=$(cat /etc/xray/username)
BOX_WIDTH=48
TEXT="SCRIPT BY $nama"
TEXT_LENGTH=${#TEXT}
LEFT_PADDING=$(( (BOX_WIDTH - TEXT_LENGTH) / 2 ))
RIGHT_PADDING=$(( BOX_WIDTH - TEXT_LENGTH - LEFT_PADDING ))
LEFT_SPACE=$(printf '%*s' $LEFT_PADDING '')
RIGHT_SPACE=$(printf '%*s' $RIGHT_PADDING '')
print_rainbow " ════════════════════════════════════════════════"
print_rainbow " ${LEFT_SPACE}${TEXT}${RIGHT_SPACE}"
print_rainbow " ════════════════════════════════════════════════"
read -p "Tekan Enter untuk kembali ke menu..."
m-ftr
exit
}

KIRI="\033[1;32m>\033[1;33m>\033[1;31m>\033[1;31m$NC"
ipsaya=$(curl -sS ipv4.icanhazip.com)
data_server=$(curl -v --insecure --silent https://google.com/ 2>&1 | grep Date | sed -e 's/< Date: //')
date_list=$(date +"%Y-%m-%d" -d "$data_server")
data_ip="https://raw.githubusercontent.com/kertasbaru/izin/main/ip"
checking_sc() {
  useexp=$(wget -qO- $data_ip | grep -wE $ipsaya | awk '{print $3}')

  if [[ "$useexp" == "Lifetime" ]]; then
    # Jika useexp adalah "Lifetime", anggap tetap aktif

    return
  fi
  date_list_epoch=$(date -d "$date_list" +%s)
  useexp_epoch=$(date -d "$useexp" +%s 2>/dev/null)

  if [[ $? -ne 0 || $date_list_epoch -gt $useexp_epoch ]]; then
    # Tanggal sekarang lebih besar dari tanggal expired, atau format salah
echo -e "\033[1;93m────────────────────────────────────────────\033[0m"
echo -e "\033[41;1m ⚠️       AKSES DI TOLAK         ⚠️ \033[0m"
echo -e "\033[1;93m────────────────────────────────────────────\033[0m"
echo -e ""
echo -e "        \033[91;1m❌ SCRIPT LOCKED ❌\033[0m"
echo -e ""
echo -e "  \033[0;33m🔒 Your VPS\033[0m $ipsaya \033[0;33mHas been Banned\033[0m"
echo -e ""
echo -e "  \033[91m⚠️  Masa Aktif Sudah Habis ⚠️\033[0m"
echo -e "  \033[0;33m💡 Beli izin resmi hanya dari Admin!\033[0m"
echo -e ""
echo -e "  \033[92;1m📞 Contact Admin:\033[0m"
echo -e "  \033[96m🌍 Telegram: https://t.me/WuzzSTORE\033[0m"
echo -e "  \033[96m📱 WhatsApp: https://wa.me/6287760204418\033[0m"
echo -e ""
echo -e "\033[1;93m────────────────────────────────────────────\033[0m"
exit 1
  fi
}
checking_sc

# // System Information
domain=$(cat /etc/xray/domain)
WKT=$(curl -s ipinfo.io/timezone)
IPVPS=$(curl -s ipv4.icanhazip.com)
tram=$(free -m | awk 'NR==2 {print $2}')
swap=$(free -m | awk 'NR==4 {print $2}')
freq=$(awk -F: ' /cpu MHz/ {freq=$2} END {print freq}' /proc/cpuinfo)
cores=$(awk -F: '/model name/ {core++} END {print core}' /proc/cpuinfo)
cname=$(awk -F: '/model name/ {name=$2} END {print name}' /proc/cpuinfo)
knr=$( uname -r )

clear
# // Exporting IP Address
export IP=$( curl -s https://ipinfo.io/ip/ )
clear
# // SSH
ssh_service=$(/etc/init.d/ssh status | grep Active | awk '{print $3}' | cut -d "(" -f2 | cut -d ")" -f1)
openvpn_service="$(systemctl show openvpn.service --no-page)"
oovpn=$(echo "${openvpn_service}" | grep 'ActiveState=' | cut -f2 -d=)
dropbear_status=$(/etc/init.d/dropbear status | grep Active | awk '{print $3}' | cut -d "(" -f2 | cut -d ")" -f1)
haproxy_service=$(systemctl status haproxy | grep Active | awk '{print $3}' | cut -d "(" -f2 | cut -d ")" -f1)
#noobz_service=$(systemctl status noobzvpns | grep Active | awk '{print $3}' | cut -d "(" -f2 | cut -d ")" -f1)
fail2ban_service=$(/etc/init.d/fail2ban status | grep Active | awk '{print $3}' | cut -d "(" -f2 | cut -d ")" -f1)
cron_service=$(systemctl  status cron | grep Active | awk '{print $3}' | cut -d "(" -f2 | cut -d ")" -f1)
badvpn1=$(systemctl status badvpn1 | grep Active | awk '{print $3}' | cut -d "(" -f2 | cut -d ")" -f1)
badvpn2=$(systemctl status badvpn2 | grep Active | awk '{print $3}' | cut -d "(" -f2 | cut -d ")" -f1)
badvpn3=$(systemctl status badvpn3 | grep Active | awk '{print $3}' | cut -d "(" -f2 | cut -d ")" -f1)
Iptables=$(systemctl status netfilter-persistent | grep active | awk '{print $3}' | sed 's/(//g' | sed 's/)//g')
RClocal=$(systemctl status rc-local | grep active | awk '{print $3}' | sed 's/(//g' | sed 's/)//g')
Autorebot=$(systemctl status rc-local | grep active | awk '{print $3}' | sed 's/(//g' | sed 's/)//g')
UdpSSH=$(systemctl status udp-custom | grep active | awk '{print $3}' | sed 's/(//g' | sed 's/)//g')
openssh=$( systemctl status ssh | grep Active | awk '{print $3}' | sed 's/(//g' | sed 's/)//g' )
clear

if [[ $openssh == "running" ]]; then
    status_openssh="\033[92;1m[ON]$NC"
else
    status_openssh="\033[91;1m[OFF]${NC} "
fi

# // SSH Websocket Proxy
ssh_ws=$( systemctl status ws | grep Active | awk '{print $3}' | sed 's/(//g' | sed 's/)//g' )
if [[ $ssh_ws == "running" ]]; then
    status_ws_epro="\033[92;1m[ON]$NC"
else
    status_ws_epro="\033[91;1m[OFF]${NC} "
fi

# // Trojan Proxy
ss=$( systemctl status xray | grep Active | awk '{print $3}' | sed 's/(//g' | sed 's/)//g' )
if [[ $ss == "running" ]]; then
    status_ss="\033[92;1m[ON]$NC"
else
    status_ss="\033[91;1m[OFF]${NC} "
fi

# // NGINX
nginx=$( /etc/init.d/nginx status | grep Active | awk '{print $3}' | sed 's/(//g' | sed 's/)//g' )
if [[ $nginx == "running" ]]; then
    status_nginx="\033[92;1m[ON]$NC"
else
    status_nginx="\033[91;1m[OFF]${NC} "
fi

# STATUS SERVICE IPTABLES
if [[ $Iptables == "exited" ]]; then
    status_galo="\033[92;1m[ON]$NC"
else
    status_galo="\033[91;1m[OFF]${NC}"
fi

# STATUS SERVICE  SSH 
if [[ $ssh_service == "running" ]]; then 
   status_ssh="\033[92;1m[ON]$NC"
else
   status_ssh="\033[91;1m[OFF]${NC} "
fi

# STATUS NOOBZVPNS SSH 
if [[ $noobz_service == "running" ]]; then 
   status_noobz="\033[92;1m[ON]$NC"
else
   status_noobz="\033[91;1m[OFF]${NC} "
fi

# STATUS OHP SSH
#if [[ $ohr == "running" ]]; then 
 #  sohr="\033[92;1m[ON]${NC}${NC}"
#else
 #  sohr="\033[91;1m[OFF]${NC} ${NC}"
#fi

# STATUS SERVICE OPENVPN
if [[ $oovpn == "active" ]]; then
  status_openvpn="\033[92;1m[ON]$NC"
else
  status_openvpn="\033[91;1m[OFF]${NC} "
fi

# STATUS SERVICE DROPBEAR
if [[ $dropbear_status == "running" ]]; then 
   status_beruangjatuh="\033[92;1m[ON]$NC"
else
   status_beruangjatuh="\033[91;1m[OFF]${NC} ${NC}"
fi

# STATUS SERVICE HAPROXY
if [[ $haproxy_service == "running" ]]; then 
   status_haproxy="\033[92;1m[ON]$NC"
else
   status_haproxy="\033[91;1m[OFF]${NC} "
fi

# STATUS SERVICE  SQUID 
#if [[ $squid_service == "running" ]]; then 
  # status_squid="\033[92;1m[ON]${NC} ( No Etror )"
#else
  # status_squid="\033[91;1m[OFF]${NC} "
#fi

# STATUS SERVICE  FAIL2BAN 
if [[ $fail2ban_service == "running" ]]; then 
   status_fail2ban="\033[92;1m[ON]$NC"
else
   status_fail2ban="\033[91;1m[OFF]${NC} "
fi

# STATUS SERVICE  CRONS 
if [[ $cron_service == "running" ]]; then 
   status_cron="\033[92;1m[ON]$NC"
else
   status_cron="\033[91;1m[OFF]${NC} "
fi

# STATUS SERVICE  BADVPN 1
if [[ $badvpn1 == "running" ]]; then 
   status_udp1="\033[92;1m[ON]$NC"
else
   status_udp1="\033[91;1m[OFF]${NC} "
fi

# STATUS SERVICE  BADVPN 2
if [[ $badvpn2 == "running" ]]; then 
   status_udp2="\033[92;1m[ON]$NC"
else
   status_udp2="\033[91;1m[OFF]${NC} "
fi

# STATUS SERVICE  BADVPN 3
if [[ $badvpn3 == "running" ]]; then 
   status_udp3="\033[92;1m[ON]$NC"
else
   status_udp3="\033[91;1m[OFF]${NC} "
fi

if [[ $RClocal == "exited" ]]; then
    status_galoo="\033[92;1m[ON]$NC"
else
    status_galoo="\033[91;1m[OFF]${NC}"
fi

if [[ $Autorebot == "exited" ]]; then
    status_galooo="\033[92;1m[ON]$NC"
else
    status_galooo="\033[91;1m[OFF]${NC}"
fi

# STATUS SERVICE  SSH UDP 
if [[ $UdpSSH == "running" ]]; then 
   status_udp="\033[92;1m[ON]$NC"
else
   status_udp="\033[91;1m[OFF]${NC} "
fi

# // Running Function 
clear

Newbie_Banner
garis_atas
echo -e "  ${WHITEBLD}Service SSH / TUN                 $blue=$NC $status_ssh"
echo -e "  ${WHITEBLD}Service SSH UDP                   $blue=$NC $status_udp"
#echo -e " ${WHITEBLD}Service SSH NOOBZVPNS             $blue=$NC $status_noobz"
echo -e "  ${WHITEBLD}Service OpenVPN SSL               $blue=$NC $status_openvpn"
echo -e "  ${WHITEBLD}Service OpenVPN WS-SSL            $blue=$NC $status_openvpn"
echo -e "  ${WHITEBLD}Service OpenVPN UDP               $blue=$NC $status_openvpn"
echo -e "  ${WHITEBLD}Service OpenVPN TCP               $blue=$NC $status_openvpn"
echo -e "  ${WHITEBLD}Service OHP SSH                   $blue=$NC $status_openvpn"
echo -e "  ${WHITEBLD}Service OHP Dropbear              $blue=$NC $status_openvpn"
echo -e "  ${WHITEBLD}Service OHP OpenVPN               $blue=$NC $status_openvpn"
echo -e "  ${WHITEBLD}Service WS ePRO                   $blue=$NC $status_ws_epro"
echo -e "  ${WHITEBLD}Service BadVPN 7100               $blue=$NC $status_udp1"
echo -e "  ${WHITEBLD}Service BadVPN 7200               $blue=$NC $status_udp2"
echo -e "  ${WHITEBLD}Service BadVPN 7300               $blue=$NC $status_udp3"
echo -e "  ${WHITEBLD}Service Dropbear                  $blue=$NC $status_beruangjatuh"
echo -e "  ${WHITEBLD}Service Haproxy                   $blue=$NC $status_haproxy"
echo -e "  ${WHITEBLD}Service Crons                     $blue=$NC $status_cron"
echo -e "  ${WHITEBLD}Service Nginx Webserver           $blue=$NC $status_nginx"
echo -e "  ${WHITEBLD}Service Xray Vmess WS TLS         $blue=$NC $status_ss"
echo -e "  ${WHITEBLD}Service Xray Vmess WS Non TLS     $blue=$NC $status_ss"
echo -e "  ${WHITEBLD}Service Xray Vmess gRPC           $blue=$NC $status_ss"
echo -e "  ${WHITEBLD}Service Xray Vless WS TLS         $blue=$NC $status_ss"
echo -e "  ${WHITEBLD}Service Xray Vless WS Non TLS     $blue=$NC $status_ss"
echo -e "  ${WHITEBLD}Service Xray Vless gRPC           $blue=$NC $status_ss"
echo -e "  ${WHITEBLD}Service Xray Trojan WS            $blue=$NC $status_ss"
echo -e "  ${WHITEBLD}Service Xray Trojan Non WS        $blue=$NC $status_ss"
echo -e "  ${WHITEBLD}Service Xray Trojan gRPC          $blue=$NC $status_ss"
echo -e "  ${WHITEBLD}Service Xray Shadowsocks WS       $blue=$NC $status_ss"
echo -e "  ${WHITEBLD}Service Xray Shadowsocks Non WS   $blue=$NC $status_ss"
echo -e "  ${WHITEBLD}Service Xray Shadowsocks gRPC     $blue=$NC $status_ss"
garis_bawah
Sc_Credit

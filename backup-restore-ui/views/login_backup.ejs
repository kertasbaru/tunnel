<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Login Backup</title>
  <style>
    body {
      background: url('https://raw.githubusercontent.com/kertasbaru/nebot/main/Assets/banner.jpg') no-repeat center center/cover;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      margin: 0;
    }

    .login-box {
      background: rgba(255, 255, 255, 0.08);
      backdrop-filter: blur(12px);
      padding: 2rem 2.5rem;
      border-radius: 16px;
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.25);
      text-align: center;
      color: #fff;
      width: 320px;
      border: 1px solid rgba(255, 255, 255, 0.2);
    }

    h2 {
      margin-bottom: 1.5rem;
      font-weight: 600;
      letter-spacing: 1px;
      font-size: 1.4rem;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }

    input[type="password"] {
      padding: 0.8rem 1rem;
      border-radius: 10px;
      border: none;
      outline: none;
      font-size: 1rem;
      background-color: rgba(255, 255, 255, 0.15);
      color: #fff;
    }

    input::placeholder {
      color: #eee;
    }

    button {
      padding: 0.8rem 1rem;
      border: none;
      border-radius: 10px;
      background: #6c5ce7;
      color: #fff;
      font-size: 1.1rem;
      font-weight: bold;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    button:hover {
      background: #4834d4;
    }

    .back-link {
      display: block;
      margin-top: 1.5rem;
      color: #ccc;
      text-decoration: none;
      font-size: 0.95rem;
    }

    .back-link:hover {
      color: #fff;
    }

    .error-message {
      margin-bottom: 1rem;
      color: #ff6b6b;
      font-weight: bold;
      background: rgba(0, 0, 0, 0.6);
      padding: 0.5rem 1rem;
      border-radius: 6px;
    }
  </style>
</head>
<body>
  <div class="login-box">
    <h2>🔐 Login </h2>
    <% if (error) { %>
      <div class="error-message"><%= error %></div>
    <% } %>
    <form action="/backup/login" method="POST">
      <input type="password" name="password" placeholder="Masukkan Root Password" required autofocus />
      <button type="submit">Masuk</button>
    </form>
    <a class="back-link" href="https://vip.t.me/WuzzSTORE">⬅️ Kembali ke Beranda</a>
  </div>
</body>
</html>
